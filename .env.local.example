# Modal backend — copy to .env.local and replace with your deployed URL
# Deploy: modal deploy backend/modal_app.py
# Then use the URL Modal prints (e.g. https://your-workspace--real-estate-predictor.modal.run)
MODAL_BACKEND_URL=https://your-workspace--real-estate-predictor.modal.run
